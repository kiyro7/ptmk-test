## Зависимости
- Python 3.10
- Docker, docker-compose

## Запуск

- `python -m venv venv`
- `venv/Scripts/Activate` (win)
- `source venv/bin/activate` (linux/mac)
- `pip install -r requirements.txt`
- в другом терминале: `docker-compose up`
- `python main.py <mode> <args>`
